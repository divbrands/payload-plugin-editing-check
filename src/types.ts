export interface PluginTypes {
  /**
   * Enable or disable plugin
   * @default false
   */
  enabled?: boolean
  except?: string[]
}

export interface ExamplesCollectionTypes {
  someField: string
}
